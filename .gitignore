# Ignore Terraform state files (these contain sensitive data)
*.tfstate
*.tfstate.backup

# Ignore provider cache
.terraform/
.terraform.lock.hcl

# Ignore local execution plan files
terraform.tfvars
terraform.tfvars.json

# Ignore GCP credentials JSON file (VERY IMPORTANT)
key.json

# events wokflow job action